apiVersion: v1
kind: Service
metadata:
  name: auth-poc-auth-service
  labels:
    client: internal
    version: 1.0.0
    environtment: dev
  annotations:
    description: Practice Kubernetes Service
spec:
  type: LoadBalancer
  ports:
    - name: http
      nodePort: 31001
      port: 80
      targetPort: 8001
    - name: rpc
      nodePort: 31111
      port: 30001
      targetPort: 30001
  selector:
    app: auth-poc-auth