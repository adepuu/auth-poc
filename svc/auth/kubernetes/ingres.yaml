apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-poc-auth-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: auth-poc-auth-service
            port:
              number: 80