apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-poc-user-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: auth-poc-user-service
            port:
              number: 80