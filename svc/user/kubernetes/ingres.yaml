apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: auth-poc-user-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: auth-poc-user-service
          servicePort: 31002