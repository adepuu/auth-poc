apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: user-poc-user-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: user-poc-user-service
          servicePort: 8002